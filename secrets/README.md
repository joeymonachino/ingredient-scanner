Local secrets live in this folder and are ignored by git.

For local OCR with Google Vision, create one of these files:
- google_cloud_vision_api_key.txt
- google_vision_api_key.txt

Put only the raw API key in the file with no extra quotes.
